# Sources are submodules; build output never enters git — it goes to GitHub Releases.
/.deps*
/out/
*.o
*.a

# Signing material never belongs here — it lives in the release environment's secrets.
*.p12
*.cer
*.keychain-db
